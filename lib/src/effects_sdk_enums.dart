enum SegmentationPreset { quality, balanced, speed, lightning }

enum FitMode { fill, fit }

enum BoundaryMode { smooth, strong }

enum Layout { center, leftBottom, rightBottom }

enum MLBackend { auto, cpu, gpu }

enum Placement { 
  topLeft,
  bottomLeft,
  center,
  topRight,
  bottomRight,
  custom
}

enum FrameFormat { rgbx, i420 }